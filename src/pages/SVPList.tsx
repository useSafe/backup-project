import React from 'react';
import ProcurementList from './ProcurementList';

const SVPList: React.FC = () => {
    return <ProcurementList forcedType="SVP" pageTitle="SVP Records" />;
};

export default SVPList;
